
const Section = ({children}) => {
    return (
        <section>
            <h2 className="feedbackTitle">Please leave feedback</h2>
            {children}
        </section>
    )
}

export default Section;